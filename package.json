{
  "name": "excel-edit",
  "version": "1.0.0",
  "description": "Excel Web Editor - Backend",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
